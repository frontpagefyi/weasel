{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "globalEnv": ["CI", "NODE_ENV"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [".next/**", "!.next/cache/**", "dist/**"]
    },
    "typegen": {
      "dependsOn": ["^build"],
      "outputs": [".next/types/**"]
    },
    "lint": {
      "dependsOn": ["^lint", "typegen"]
    },
    "type-check": {
      "dependsOn": ["^type-check", "typegen"],
      "outputs": ["eslint_report.json"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "test": {
      "dependsOn": ["^test"]
    }
  }
}
